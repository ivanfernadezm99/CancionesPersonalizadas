# OpenSpec Configuration — CancionesPersonalizadas
# Auto-generated by sdd-init on 2026-07-27

context: >
  Greenfield project: AI-powered personalized romantic song generator in Spanish.
  Backend: Python 3.10 + FastAPI + Uvicorn, generates songs via Google Lyria 3
  (OpenClaw gateway). Frontend: TBD (likely web SPA). Output: mp3/wav with
  personalized lyrics and instrumental. Inspired by "Notas Infinitas" but fully AI.

strict_tdd: true

testing:
  runner: pytest 9.0.3
  layers:
    unit: pytest
    integration: pytest + httpx
    e2e: none (TBD — Playwright available)
  coverage: pytest-cov 7.1.0
  linter: ruff 0.15.16
  type_checker: mypy 2.1.0
  formatter: black 26.5.1

phases:
  propose: true
  spec: true
  design: true
  tasks: true
  apply: true
  verify: true
  archive: true

session:
  pace: auto
  pr_strategy: auto-forecast
  review_budget: 800
